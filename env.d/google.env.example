# Google plugins configuration (Calendar, Docs, Drive, Gmail)
#
# Google plugins require OAuth2 tokens acquired via a browser
# consent flow. Tokens are stored in the credentials directory.
#
# No env vars are needed if using the default credential path.
# Only set GOOGLE_CREDENTIALS_DIR if you store credentials
# elsewhere:

#GOOGLE_CREDENTIALS_DIR=/custom/path/to/google/credentials
